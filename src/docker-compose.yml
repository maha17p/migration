services:
  migration-db:
    image: postgres:15
    environment:
      POSTGRES_DB: user
      POSTGRES_USER: admin_user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: